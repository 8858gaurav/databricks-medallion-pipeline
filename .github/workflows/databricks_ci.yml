name: Databricks Medallion CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  python-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: pip install pytest
      - name: Run Unit Tests
        run: pytest tests/

  deploy-to-databricks:
    runs-on: ubuntu-latest
    needs: python-validation
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v3

      - name: Install Databricks CLI
        uses: databricks/setup-cli@main

      # This step helps us debug by showing exactly where the files are
    # Updated Debug Step
      - name: List files for debugging
        run: |
          echo "Current directory: $(pwd)"
          ls -F
          echo "Searching for the bundle config..."
          find . -name "databricks.yml"

      - name: Bundle Deploy and Run
        # We must deploy first so Databricks has the latest code, 
        # then we run the job to see it in action.
        run: |
          databricks bundle deploy -t prod
          databricks bundle run JOB_via_GitHUb -t prod
        env:
          DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}