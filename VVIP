here, we are just reading the file from different containers : customers/orders as a raw data.
we are not creating bronzr table via delta format table techniques with TBLPROPERTIES(delta.enableChangeDataFeed = true). 
we are just writing an output under broze layer (bronze container)

also, TBLPROPERTIES(delta.enableChangeDataFeed = true); is not applicable here 
